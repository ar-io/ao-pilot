print("Loading .busted configuration...")
return {
    default = {
        root = ".",
        pattern = ".*_spec.lua$",
        helper = "contract/spec/setup.lua",
        verbose = true,
        coverage = true,
        output = "utfTerminal"
  }
}
