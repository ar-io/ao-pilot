export function locateWith({ loadProcessScheduler, cache, followRedirects, checkForRedirect }: {
    loadProcessScheduler: any;
    cache: any;
    followRedirects: any;
    checkForRedirect: any;
}): (process: string) => Promise<{
    url: string;
    address: string;
}>;
