print("Loading .busted configuration...")
return {
    default = {
        root = "contract",
        pattern = ".*_spec.lua$",
        helper = "contract/spec/setup.lua",
        verbose = true,
        coverage = false,
        output = "utfTerminal",
        jobs = 4
    }
}
