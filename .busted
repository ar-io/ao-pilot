print("Loading .busted configuration...")
return {
    default = {
        root = ".",
        pattern = ".*.spec.lua",
        helper = "contract/spec/setup.lua",
        verbose = true,
        coverage = true,
        output = "utfTerminal"
  }
}
