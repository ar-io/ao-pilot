print("Loading .busted configuration...")
return {
    default = {
        root = ".",
        pattern = "**/*_spec.lua$",
        helper = "spec/setup.lua",
        verbose = true,
        coverage = true,
        output = "utfTerminal",
        jobs = 4,
    }
}
